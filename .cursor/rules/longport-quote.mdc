---
description: 当用户指令涉及股票涨跌幅时，默认使用 LongPort API 获取行情
alwaysApply: true
---

# 股票涨跌幅数据来源

当用户的指令中提到**股票涨跌幅**、**盘前/盘后涨跌**、**股价**、**行情**等时，默认使用项目内的 **LongPort API** 获取实时或盘前/盘后数据。

- 凭证从项目根目录的 `.env` 读取（`LONGPORT_APP_KEY`、`LONGPORT_APP_SECRET`、`LONGPORT_ACCESS_TOKEN`）。
- 可用的调用方式：在项目根运行 `quote/quote_premarket.py`、`quote/premarket_top_gainers.py`，或基于 LongPort SDK 编写/运行脚本（如批量标的、涨跌幅排序等）。
- 仅在用户明确要求用其他来源（如网页、其他 API）时，才改用非 LongPort 方式。

## 盘前涨跌幅算法（固定，后续保持不变）

**只要是盘前，都用上一交易日的收盘价来算涨跌幅。**

- 实现方式：用日 K 最近一根（已结束交易日）的收盘价作为「昨收」，再与盘前最新价计算涨跌幅，与 LongBridge Pro 口径一致。
- LongPort 的 `quote.prev_close` 可能与「上一交易日收盘」不一致，盘前场景下**不得**直接使用 `prev_close` 算涨幅，必须用 `history_candlesticks_by_offset(symbol, Period.Day, AdjustType.NoAdjust, False, 2)` 取最后一根 K 的 `close`。
- 项目内已遵守此算法的脚本：`quote/quote_premarket.py`、`quote/premarket_top_gainers.py`、`hotspot_monitor/hotspot_monitor_api.py`（`fetch_quotes`）。
