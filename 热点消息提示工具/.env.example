# 热点摘要 LLM 配置（复制为 .env 并填入真实值，不要提交 .env 到 git）
# 任一大模型只要提供 OpenAI 兼容接口即可（OpenAI / DeepSeek / 国内中转等）

# API Key（必填才能用 --llm 自动出摘要）
OPENAI_API_KEY=sk-xxxxxxxx

# 可选：非 OpenAI 官方时填兼容接口地址
# OPENAI_API_BASE=https://api.deepseek.com
# OPENAI_MODEL=deepseek-chat
# OPENAI_MODEL=gpt-4o-mini
